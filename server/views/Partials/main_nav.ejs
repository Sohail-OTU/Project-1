<!--NavBar-->
<nav class="navbar navbar-expand-lg navbar-light bg-mint-green">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><img class="logo_ rounded-circle" src="/Assets/images/Sortify Logo.jpg" alt="Logo"> Sortify</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <% if(title=="Sortify") { %>
              <a class="nav-link active" href="/home">
            <% } else {%>
              <a class="nav-link" href="/home">
            <% } %>  
            <i class="fa-solid fa-house"></i> Home</a>
          </li>
          <li class="nav-item">
            <% if(title=="About Us") { %>
              <a class="nav-link active" href="/aboutus">
            <% } else {%>
              <a class="nav-link" href="/aboutus">  
            <% } %>       
              <i class="fa-solid fa-circle-question"></i> About Us</a>
          </li>
          <li class="nav-item">
            <% if(title=="Service") { %>
              <a class="nav-link active" href="/service">
            <% } else {%> 
              <a class="nav-link" href="/service">
            <% } %>  
                <i class="fa-solid fa-hand-holding-heart"></i> Services</a>
          </li>
          <li class="nav-item">
            <% if(
              title=="Task" || title=="Active Tasks" || title=="Completed Tasks" ||
              title=="Add Task" || title=="Add Active Task" || title=="Edit an Active Task"
            ) { %>
              <a class="nav-link active" href="/tasks">
            <% } else {%>
              <a class="nav-link" href="/tasks">
            <% } %>  
            <i class="fa-regular fa-folder-open"></i> Task Organizer</a>
          </li>
          <li class="nav-item"></li>
            <% if(title=="Contact Us") { %>
              <a class="nav-link active" href="/contactus">
            <% } else {%>  
              <a class="nav-link" href="/contactus">
            <% } %> 
            <i class="fa-solid fa-phone-volume"></i> ContactUs</a>
          </li>
          </li>
            <% if (user) { %>
              <li class="nav-item">
                <a class="nav-link" href="/logout">
                  <i class="fa-solid fa-right-from-bracket"></i> Logout
                </a>
              </li>
            <% } else { %>
              <li class="nav-item">
                <a class="nav-link" href="/login">
                  <i class="fa-solid fa-lock"></i> Login
                </a>
              </li>
            <% } %>
        </ul>
      </div>
    </div>
  </nav>